<!DOCTYPE html>
<html lang="fr">

<head>
  <meta charset="UTF-8">
  <!-- <meta http-equiv="refresh" content="2"> -->
  <meta name="theme-color" content="#293847">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">


  <!-- awesome cdn  -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css" integrity="sha512-z3gLpd7yknf1YoNbCzqRKc4qyor8gaKU1qmn+CShxbuBusANI9QpRohGBreCFkKxLhei6S9CQXFEbbKuqLg0DA==" crossorigin="anonymous" referrerpolicy="no-referrer" />

  <!-- box icon here  -->
  <link href='https://unpkg.com/boxicons@2.1.4/css/boxicons.min.css' rel='stylesheet'>

  <!-- boostrap here -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">


  <!-- mon css here -->
  <link rel="stylesheet" href="/css/vehicule.css">
  <title>Appartements</title>
</head>

<body id="body">

  <%- include('composants/nav.ejs') %>

  <%- include('composants/burger-menu.ejs') %>
  <%- include('composants/panier.ejs') %>




  <h4 style="text-align: start;"><strong>Appartements</strong></h4>
  <div class="main">
    
    <br>


  </div>


  <!-- FOOTER ICI  -->
  <%- include('composants/footer.ejs') %>
  <!-- FOOTER ICI  -->



  <!-- boostrap here -->

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL" crossorigin="anonymous"></script>

  <script src="/script/index.js"></script>

</body>

<script>
  document.addEventListener('DOMContentLoaded', function() {

    // 1. Récupérez les données depuis le serveur et stockez-les dans le localStorage
    fetch('/recupererappart')
      .then(response => {
        if (!response.ok) {
          throw new Error('La réponse du serveur n\'est pas OK');
        }
        return response.json();
      })
      .then(data => {
        localStorage.setItem('appart', JSON.stringify(data));
        console.log(data);
        // 2. Une fois les données récupérées, bouclez dans votre slider
        populateappart();
      })
      .catch(error => console.error('Erreur lors de la récupération des données appart : ' + error));

    // 3. Créez une fonction pour boucler dans le slider
    function populateappart() {
      // Récupérer les données d'articles visités depuis le localStorage
      const appart = JSON.parse(localStorage.getItem('appart'));

      // Vérifiez si des articles visités existent
      if (appart && appart.length > 0) {
        // Sélectionnez l'élément parent où vous souhaitez ajouter les articles
        const appartContainer = document.querySelector('.main');

        // Bouclez sur les articles et générez dynamiquement les éléments HTML
        appart.forEach((appartCardItem) => {
          const appartDiv = document.createElement('div');
          appartDiv.classList.add('carte');

          appartDiv.innerHTML = `
          <div class="carte-top">

<div class="left">
    <p><strong>${appartCardItem.nom}</strong></p>
    <span>${appartCardItem.infos}</span> <br> <br>
    <span><strong><i class="fa-solid fa-location-dot"></i> ${appartCardItem.localisation}</strong></span>
</div>
<div class="right">
    <img src="${appartCardItem.url}" width="130" alt="voiture">
</div>

</div>
<hr>
<h5>${appartCardItem.prix} CFA / Jour</h5>
      `;

          appartContainer.appendChild(appartDiv);
        });
      }
    }
  })
</script>

</html>